import React from "react";
import { SectionWrapper } from "../hoc";

const ProjectCard = ({}) => {
  return <></>;
};

const Works = () => {
  return <></>;
};

export default SectionWrapper(Works, "");
